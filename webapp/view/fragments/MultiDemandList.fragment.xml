<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core" xmlns:form="sap.ui.layout.form">
	<Dialog title="{i18n>xtit.demandAssignments}" resizable="true" draggable="true" contentWidth="50%">
		<Table id="idFragMultiDemandListTable" items="{ path: 'multiDemandModel>/data',sorter : [{ path : 'ResourceName', group:true  }] }">
			<columns>
				<Column demandPopin="true">
					<Text text="{i18n>xtit.demands}"/>
				</Column>
				<Column demandPopin="true">
					<Text text="{i18n>lbl.fromdate}"/>
				</Column>
				<Column demandPopin="true">
					<Text text="{i18n>lbl.todate}"/>
				</Column>
				<Column demandPopin="true">
					<Text text="{i18n>lbl.status}"/>
				</Column>
			</columns>
			<items>
				<ColumnListItem>
					<cells>
						<Text text="{multiDemandModel>DemandDesc}"/>
						<Text text="{path:'multiDemandModel>DateFrom',formatter:'.formatter.dateWithTime'}"/>
						<Text text="{path:'multiDemandModel>DateTo',formatter:'.formatter.dateWithTime'}"/>
						<Text text="{multiDemandModel>Status}"/>
					</cells>
				</ColumnListItem>
			</items>
		</Table>
		<beginButton>
			<Button text="{i18n>btn.procced}" press="onMultiDemandProceed" type="Emphasized"/>
		</beginButton>
		<endButton>
			<Button text="{i18n>btn.close}" press="oMultinDemandDialogClose" type="Emphasized"/>
		</endButton>
	</Dialog>
</core:FragmentDefinition>